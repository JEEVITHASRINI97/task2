export function ColorBox({ color }) {
  const styles = {
    background: color,
    width: "300px",
    height: "40px",
    margin: "10px"
  };

  return (
    <div style={styles}>

    </div>
  );

}
